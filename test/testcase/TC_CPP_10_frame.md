# TC_CPP_10 — 帧数据访问

## 文档版本记录

| 版本 | 日期 | 作者 | 变更说明 |
|------|------|------|---------|
| 1.0 | 2026-03-17 | — | 初始版本，包含 14 个测试用例 |

---

## 关联功能点

本模块覆盖 [sdk_feature_matrix.md](../sdk_feature_matrix.md) 中 **模块 10：帧数据访问** 的全部功能点。

---

## TC_CPP_10_01 — Frame 基本属性完整性

### 用例版本记录

| 版本 | 日期 | 变更说明 |
|------|------|---------|
| 1.0 | 2026-03-17 | 初始版本 |

### 基本信息

| 字段 | 值 |
|------|-----|
| 编号 | TC_CPP_10_01 |
| 名称 | Frame 基本属性完整性 |
| 目的 | 验证 Frame 基本属性（序号、格式、类型、数据）完整有效 |
| 覆盖功能点 | 10.1-010~10.1-030, 10.1-070, 10.1-080 |
| 前置条件 | 至少一台设备通过 USB 连接 |
| 硬件依赖 | 是 |
| 优先级 | P0 |

### 测试步骤

1. 启动 Pipeline 获取帧
2. 读取 Frame 的各项基本属性

### 检查项

- ✅ `index() >= 0`
- ✅ `format()` 返回有效的 OBFormat 枚举值
- ✅ `type()` 返回有效的 OBFrameType 枚举值
- ✅ `data()` 返回非空数据指针
- ✅ `dataSize() > 0`

### 预期结果

Frame 所有基本属性均有效，数据可访问。

---

## TC_CPP_10_02 — Frame 时间戳三元组与单调性

### 用例版本记录

| 版本 | 日期 | 变更说明 |
|------|------|---------|
| 1.0 | 2026-03-17 | 初始版本 |

### 基本信息

| 字段 | 值 |
|------|-----|
| 编号 | TC_CPP_10_02 |
| 名称 | Frame 时间戳三元组与单调性 |
| 目的 | 验证 Frame 三种时间戳有效且时间戳单调递增 |
| 覆盖功能点 | 10.1-040~10.1-060 |
| 前置条件 | 至少一台设备通过 USB 连接 |
| 硬件依赖 | 是 |
| 优先级 | P0 |

### 测试步骤

1. 启动 Pipeline 持续获取帧
2. 连续 30 帧记录三种时间戳
3. 验证时间戳单调性

### 检查项

- ✅ `timeStampUs() > 0`（设备时间戳）
- ✅ `systemTimeStampUs() > 0`（系统时间戳）
- ✅ `globalTimeStampUs() > 0`（全局时间戳）
- ✅ 连续帧 `timeStampUs()` 严格递增
- ✅ 时间戳差值符合帧率预期（30fps 约 33333us）

### 预期结果

三种时间戳均有效，设备时间戳连续递增。

---

## TC_CPP_10_03 — Frame 关联信息

### 用例版本记录

| 版本 | 日期 | 变更说明 |
|------|------|---------|
| 1.0 | 2026-03-17 | 初始版本 |

### 基本信息

| 字段 | 值 |
|------|-----|
| 编号 | TC_CPP_10_03 |
| 名称 | Frame 关联信息 |
| 目的 | 验证 Frame 可获取关联的 StreamProfile、Sensor、Device |
| 覆盖功能点 | 10.1-090~10.1-110 |
| 前置条件 | 至少一台设备通过 USB 连接 |
| 硬件依赖 | 是 |
| 优先级 | P1 |

### 测试步骤

1. 启动 Pipeline 获取帧
2. 调用 `frame->getStreamProfile()`
3. 调用 `frame->getSensor()`
4. 调用 `frame->getDevice()`

### 检查项

- ✅ `getStreamProfile()` 返回非空 Profile，与开流配置一致
- ✅ `getSensor()` 返回非空 Sensor
- ✅ `getDevice()` 返回非空 Device
- ✅ Sensor 类型与 Frame 类型对应

### 预期结果

Frame 可正确获取关联的 Profile、Sensor、Device 信息。

---

## TC_CPP_10_04 — Frame 引用计数

### 用例版本记录

| 版本 | 日期 | 变更说明 |
|------|------|---------|
| 1.0 | 2026-03-17 | 初始版本 |

### 基本信息

| 字段 | 值 |
|------|-----|
| 编号 | TC_CPP_10_04 |
| 名称 | Frame 引用计数 |
| 目的 | 验证 C API 帧引用计数功能 |
| 覆盖功能点 | 10.1-120 |
| 前置条件 | SDK 库可用 |
| 硬件依赖 | 否 |
| 优先级 | P1 |

### 测试步骤

1. 创建 Frame（或通过其他方式获取）
2. 调用 C API `ob_frame_add_ref()` 增加引用
3. 删除原始引用
4. 验证通过新引用仍可访问帧
5. 删除新引用

### 检查项

- ✅ `ob_frame_add_ref()` 调用成功
- ✅ 引用计数正确增加
- ✅ 删除一个引用后帧数据仍可访问
- ✅ 所有引用删除后帧正确释放，无崩溃

### 预期结果

帧引用计数机制工作正常，内存安全。

---

## TC_CPP_10_05 — VideoFrame 宽高与像素属性

### 用例版本记录

| 版本 | 日期 | 变更说明 |
|------|------|---------|
| 1.0 | 2026-03-17 | 初始版本 |

### 基本信息

| 字段 | 值 |
|------|-----|
| 编号 | TC_CPP_10_05 |
| 名称 | VideoFrame 宽高与像素属性 |
| 目的 | 验证 VideoFrame 的宽高和像素属性 |
| 覆盖功能点 | 10.2-010~10.2-040 |
| 前置条件 | 至少一台设备通过 USB 连接 |
| 硬件依赖 | 是 |
| 优先级 | P0 |

### 测试步骤

1. 启动 Pipeline 获取深度/彩色/IR 帧
2. 将 Frame 转换为 VideoFrame
3. 读取宽高和像素属性

### 检查项

- ✅ `width() > 0`
- ✅ `height() > 0`
- ✅ `pixelType()` 返回有效的像素类型
- ✅ `pixelAvailableBitSize() > 0`
- ✅ 宽高值与 StreamProfile 配置一致

### 预期结果

VideoFrame 的宽高和像素属性正确，与配置匹配。

---

## TC_CPP_10_06 — DepthFrame 深度比例与数据有效性

### 用例版本记录

| 版本 | 日期 | 变更说明 |
|------|------|---------|
| 1.0 | 2026-03-17 | 初始版本 |

### 基本信息

| 字段 | 值 |
|------|-----|
| 编号 | TC_CPP_10_06 |
| 名称 | DepthFrame 深度比例与数据有效性 |
| 目的 | 验证 DepthFrame 深度比例和数据有效性 |
| 覆盖功能点 | 10.3-010 |
| 前置条件 | 至少一台设备通过 USB 连接 |
| 硬件依赖 | 是 |
| 优先级 | P0 |

### 测试步骤

1. 启动深度流
2. 获取深度帧
3. 读取深度比例
4. 分析深度数据

### 检查项

- ✅ `getValueScale() > 0`（典型值 0.1 或 1.0，单位 mm）
- ✅ 深度数据非全零
- ✅ 至少 10% 像素有有效深度值（> 0）
- ✅ 深度值在合理范围内（如 100mm ~ 10000mm）

### 预期结果

深度比例有效，深度数据包含有效测量值。

---

## TC_CPP_10_07 — ColorFrame / IRFrame 数据有效性

### 用例版本记录

| 版本 | 日期 | 变更说明 |
|------|------|---------|
| 1.0 | 2026-03-17 | 初始版本 |

### 基本信息

| 字段 | 值 |
|------|-----|
| 编号 | TC_CPP_10_07 |
| 名称 | ColorFrame / IRFrame 数据有效性 |
| 目的 | 验证彩色帧和 IR 帧数据非全零 |
| 覆盖功能点 | 10.2-010 |
| 前置条件 | 至少一台设备通过 USB 连接 |
| 硬件依赖 | 是 |
| 优先级 | P0 |

### 测试步骤

1. 启动彩色流和 IR 流
2. 获取彩色帧和 IR 帧
3. 检查数据内容

### 检查项

- ✅ ColorFrame 数据非全零
- ✅ IRFrame 数据非全零
- ✅ 像素值分布合理（非单一值）

### 预期结果

彩色帧和 IR 帧数据有效，包含实际图像内容。

---

## TC_CPP_10_08 — PointsFrame 属性

### 用例版本记录

| 版本 | 日期 | 变更说明 |
|------|------|---------|
| 1.0 | 2026-03-17 | 初始版本 |

### 基本信息

| 字段 | 值 |
|------|-----|
| 编号 | TC_CPP_10_08 |
| 名称 | PointsFrame 属性 |
| 目的 | 验证点云帧的坐标缩放和尺寸属性 |
| 覆盖功能点 | 10.4-010, 10.4-020 |
| 前置条件 | 至少一台设备通过 USB 连接 |
| 硬件依赖 | 是 |
| 优先级 | P1 |

### 测试步骤

1. 使用 PointCloudFilter 生成点云帧
2. 读取 PointsFrame 属性

### 检查项

- ✅ `getCoordinateValueScale() > 0`
- ✅ `width() > 0`
- ✅ `height() > 0`
- ✅ 点云数据非空

### 预期结果

PointsFrame 属性正确，包含有效点云数据。

---

## TC_CPP_10_09 — AccelFrame 值与温度

### 用例版本记录

| 版本 | 日期 | 变更说明 |
|------|------|---------|
| 1.0 | 2026-03-17 | 初始版本 |

### 基本信息

| 字段 | 值 |
|------|-----|
| 编号 | TC_CPP_10_09 |
| 名称 | AccelFrame 值与温度 |
| 目的 | 验证加速度计帧数据和温度 |
| 覆盖功能点 | 10.5-010, 10.5-030 |
| 前置条件 | 至少一台 Gemini 335 设备通过 USB 连接 |
| 硬件依赖 | 是 |
| 优先级 | P0 |

### 测试步骤

1. 启动加速度计流
2. 获取 AccelFrame
3. 读取加速度和温度值

### 检查项

- ✅ `value()` 返回 {x, y, z} 三个分量
- ✅ 静止时 z ≈ 9.8 m/s²（重力加速度）
- ✅ x, y 接近 0
- ✅ `temperature()` 在 0~80°C 范围内

### 预期结果

加速度数据合理，温度在正常范围内。

---

## TC_CPP_10_10 — GyroFrame 值与温度

### 用例版本记录

| 版本 | 日期 | 变更说明 |
|------|------|---------|
| 1.0 | 2026-03-17 | 初始版本 |

### 基本信息

| 字段 | 值 |
|------|-----|
| 编号 | TC_CPP_10_10 |
| 名称 | GyroFrame 值与温度 |
| 目的 | 验证陀螺仪帧数据和温度 |
| 覆盖功能点 | 10.5-020, 10.5-030 |
| 前置条件 | 至少一台 Gemini 335 设备通过 USB 连接 |
| 硬件依赖 | 是 |
| 优先级 | P0 |

### 测试步骤

1. 启动陀螺仪流
2. 获取 GyroFrame
3. 读取角速度和温度值

### 检查项

- ✅ `value()` 返回 {x, y, z} 三个分量
- ✅ 静止时 x, y, z 接近 0
- ✅ `temperature()` 在合理范围内

### 预期结果

陀螺仪数据合理，静止时接近零。

---

## TC_CPP_10_11 — FrameSet 帧计数与提取

### 用例版本记录

| 版本 | 日期 | 变更说明 |
|------|------|---------|
| 1.0 | 2026-03-17 | 初始版本 |

### 基本信息

| 字段 | 值 |
|------|-----|
| 编号 | TC_CPP_10_11 |
| 名称 | FrameSet 帧计数与提取 |
| 目的 | 验证 FrameSet 的帧计数和按类型提取功能 |
| 覆盖功能点 | 10.6-010~10.6-050 |
| 前置条件 | 至少一台设备通过 USB 连接 |
| 硬件依赖 | 是 |
| 优先级 | P0 |

### 测试步骤

1. 启动多流 Pipeline
2. 获取 FrameSet
3. 读取帧计数
4. 提取各类帧

### 检查项

- ✅ `frameCount() >= 1`
- ✅ `depthFrame()` 返回有效深度帧（若启用）
- ✅ `colorFrame()` 返回有效彩色帧（若启用）
- ✅ `irFrame()` 返回有效 IR 帧（若启用）
- ✅ `pointsFrame()` 返回有效点云帧（若启用）

### 预期结果

FrameSet 正确包含所有启用的流帧，可按类型提取。

---

## TC_CPP_10_12 — FrameSet 按类型/索引提取

### 用例版本记录

| 版本 | 日期 | 变更说明 |
|------|------|---------|
| 1.0 | 2026-03-17 | 初始版本 |

### 基本信息

| 字段 | 值 |
|------|-----|
| 编号 | TC_CPP_10_12 |
| 名称 | FrameSet 按类型/索引提取 |
| 目的 | 验证 FrameSet 可按 OBFrameType 或索引提取帧 |
| 覆盖功能点 | 10.6-060, 10.6-070 |
| 前置条件 | 至少一台设备通过 USB 连接 |
| 硬件依赖 | 是 |
| 优先级 | P1 |

### 测试步骤

1. 启动多流 Pipeline
2. 获取 FrameSet
3. 使用 `getFrame(OB_FRAME_DEPTH)` 按类型提取
4. 使用 `getFrameByIndex(0)` 按索引提取

### 检查项

- ✅ `getFrame(OB_FRAME_DEPTH)` 与 `depthFrame()` 一致
- ✅ `getFrameByIndex(i)` 返回非空（i < frameCount）
- ✅ 遍历所有索引可获取所有帧

### 预期结果

FrameSet 支持按类型和索引两种方式提取帧。

---

## TC_CPP_10_13 — FrameSet 添加帧

### 用例版本记录

| 版本 | 日期 | 变更说明 |
|------|------|---------|
| 1.0 | 2026-03-17 | 初始版本 |

### 基本信息

| 字段 | 值 |
|------|-----|
| 编号 | TC_CPP_10_13 |
| 名称 | FrameSet 添加帧 |
| 目的 | 验证可创建空 FrameSet 并向其中添加帧 |
| 覆盖功能点 | 10.6-080 |
| 前置条件 | SDK 可用 |
| 硬件依赖 | 否 |
| 优先级 | P1 |

### 测试步骤

1. 调用 `FrameFactory::createFrameSet()` 创建空 FrameSet
2. 验证 `frameCount() == 0`
3. 创建或获取一个 Frame
4. 调用 `frameSet->pushFrame(frame)`
5. 验证 `frameCount()` 递增

### 检查项

- ✅ `createFrameSet()` 创建成功
- ✅ 初始 `frameCount() == 0`
- ✅ `pushFrame()` 调用成功
- ✅ `frameCount()` 增加

### 预期结果

可创建 FrameSet 并动态添加帧。

---

## TC_CPP_10_14 — FrameSet Depth+Color 帧同步验证

### 用例版本记录

| 版本 | 日期 | 变更说明 |
|------|------|---------|
| 1.0 | 2026-03-17 | 初始版本 |

### 基本信息

| 字段 | 值 |
|------|-----|
| 编号 | TC_CPP_10_14 |
| 名称 | FrameSet Depth+Color 帧同步验证 |
| 目的 | 验证启用帧同步后 Depth 和 Color 帧时间戳对齐 |
| 覆盖功能点 | 10.6-010 |
| 前置条件 | 至少一台设备通过 USB 连接 |
| 硬件依赖 | 是 |
| 优先级 | P0 |

### 测试步骤

1. 创建 Config 启用深度和彩色流
2. 启用帧同步
3. 启动 Pipeline
4. 连续获取 10 个 FrameSet
5. 比较每个 FrameSet 中 Depth 和 Color 帧的时间戳

### 检查项

- ✅ Depth 和 Color 帧时间戳差 < 33ms（一帧时间）
- ✅ 时间戳差在多数 FrameSet 中 < 10ms
- ✅ 无异常大的时间戳偏差

### 预期结果

启用帧同步后，Depth 和 Color 帧时间戳对齐，偏差在可接受范围内。
